[project]
name = "pardal"
version = "0.1.0"
description = "Web service to help with speed and latency tests."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp==3.13.2",
    "commonwealth==0.1.0",
    "loguru==0.7.3",
    "speedtest-cli==2.1.3",
]

[tool.uv]
package = false
