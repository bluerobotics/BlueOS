[project]
name = "versionchooser"
version = "0.1.0"
description = "Blue Robotics Ardusub BlueOS Version Chooser."
requires-python = ">=3.11"
dependencies = [
    "aiodocker==0.24.0",
    "aiohttp==3.12.13",
    "appdirs==1.4.4",
    "commonwealth==0.1.0",
    "connexion[aiohttp,swagger-ui]==3.2.0",
    "docker==7.1.0",
    "loguru==0.7.3",
    # This dependency needs to be locked due to error in aiohttp
    "yarl==1.20.1",
]

[tool.uv.sources]
commonwealth = { workspace = true }
